import path from "path";

const srcImagePath = path.resolve(__dirname + "/../../public/assets/srcImg");
const outImagePath = path.resolve(__dirname + "/../../public/assets/outImg");
const htmlPath = path.resolve(__dirname + "/../../public/html");
const publicPath = path.resolve(__dirname + "/../../public");
export default { srcImagePath, outImagePath, htmlPath, publicPath };
